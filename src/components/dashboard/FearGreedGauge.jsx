import React from \"react\";
import { Card, CardHeader, CardTitle, CardContent } from \"@/components/ui/card\";

export default function FearGreedGauge() {\n    const value = 72; // Extreme Greed\n    const rotation = (value / 100) * 180 - 90;\n\n    return (\n        <Card className=\"bg-card border-border\">\n            <CardHeader className=\"pb-2\">\n                <CardTitle className=\"text-sm font-medium text-muted-foreground\">Fear & Greed Index</CardTitle>\n            </CardHeader>\n            <CardContent className=\"flex flex-col items-center pt-2\">\n                <div className=\"relative w-40 h-24 overflow-hidden\">\n                    {/* Semi-circle gauge background */}\n                    <div className=\"absolute top-0 left-0 w-40 h-40 rounded-full border-[12px] border-muted opacity-30\"></div>\n                    \n                    {/* Gauge segments (simplified as border) */}\n                    <div \n                        className=\"absolute top-0 left-0 w-40 h-40 rounded-full border-[12px]\"\n                        style={{ \n                            borderColor: 'transparent',\n                            borderTopColor: value > 75 ? 'hsl(var(--primary))' : value > 50 ? '#fbbf24' : '#ef4444',\n                            transform: 'rotate(-90deg)',\n                            clipPath: `polygon(50% 50%, 0 0, ${value}% 0, 100% 100%)` // This is a placeholder for real conic gradient\n                        }}\n                    ></div>\n\n                    {/* Needle */}\n                    <div \n                        className=\"absolute bottom-0 left-1/2 w-1 h-20 bg-foreground origin-bottom transition-transform duration-1000 ease-out\"\n                        style={{ transform: `translateX(-50%) rotate(${rotation}deg)` }}\n                    >\n                        <div className=\"absolute -top-1 left-1/2 -translate-x-1/2 w-3 h-3 bg-primary rounded-full\"></div>\n                    </div>\n                </div>\n\n                <div className=\"text-center mt-2\">\n                    <div className=\"text-2xl font-black\">{value}</div>\n                    <div className=\"text-[10px] font-bold uppercase tracking-widest text-primary\">Greed</div>\n                </div>\n\n                <div className=\"flex justify-between w-full mt-4 text-[9px] text-muted-foreground font-bold\">\n                    <span>Fear</span>\n                    <span>Neutral</span>\n                    <span>Greed</span>\n                </div>\n            </CardContent>\n        </Card>\n    );\n}
